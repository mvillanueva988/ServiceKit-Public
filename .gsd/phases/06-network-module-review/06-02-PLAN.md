---
phase: 06-network-module-review
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - modules/Network.ps1
  - main.ps1
autonomous: true

must_haves:
  truths:
    - "Optimize-Network verifica cuántas propiedades de ahorro de energía se aplicaron realmente en cada adaptador"
    - "El resultado muestra al usuario 'N propiedades aplicadas' por adaptador, no solo el nombre del adaptador"
    - "Si un driver ignora ciertas propiedades, eso se refleja en el count (no se sobreestima el éxito)"
  artifacts:
    - path: "modules/Network.ps1"
      provides: "Optimize-Network con read-back de registro y ChangesMade por adaptador"
      contains: "ChangesMade"
    - path: "main.ps1"
      provides: "UI actualizada muestra 'N propiedades' por adaptador en resultado"
      contains: "propiedades aplicadas"
  key_links:
    - from: "Optimize-Network"
      to: "registro HKLM NIC subkey"
      via: "Get-ItemProperty read-back post Set-ItemProperty"
    - from: "main.ps1 resultado"
      to: "result.AdaptersOptimized"
      via: "objeto PSCustomObject con Name + ChangesMade"
---

<objective>
Mejorar Optimize-Network para verificar que los cambios de registro se aplicaron realmente, y actualizar la UI para mostrar el conteo de propiedades modificadas.

Purpose: Actualmente el resultado solo lista adaptadores procesados sin saber si los cambios tomaron efecto. Algunos drivers ignoran propiedades — el técnico debe verlo.
Output: `Optimize-Network` retorna array de objetos con `Name + ChangesMade` por adaptador. Main.ps1 muestra el conteo.
</objective>

<execution_context>
@.gsd/phases/06-network-module-review/../../../.github/skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/STATE.md
@modules/Network.ps1
@main.ps1
@.gsd/phases/06-network-module-review/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificación post-apply en Optimize-Network (Network.ps1)</name>
  <files>modules/Network.ps1</files>
  <action>
Modificar `Optimize-Network` para que, después de hacer `Set-ItemProperty` en cada propiedad, lea el valor de vuelta y cuente cuántas se aplicaron correctamente.

**Cambios dentro del foreach de propiedades** (sección `-- 2. Deshabilitar propiedades --`):

Reemplazar el bloque interno actual:
```powershell
foreach ($prop in $powerProps) {
    $regVal = Get-ItemProperty -Path $matchedKey.PSPath -Name $prop -ErrorAction SilentlyContinue
    if ($null -ne $regVal -and $null -ne $regVal.$prop) {
        Set-ItemProperty -Path $matchedKey.PSPath -Name $prop -Value '0' -ErrorAction SilentlyContinue
    }
}
$optimized.Add($adapter.Name)
```

Por:
```powershell
[int] $changesMade = 0
foreach ($prop in $powerProps) {
    $regVal = Get-ItemProperty -Path $matchedKey.PSPath -Name $prop -ErrorAction SilentlyContinue
    if ($null -ne $regVal -and $null -ne $regVal.$prop) {
        Set-ItemProperty -Path $matchedKey.PSPath -Name $prop -Value '0' -ErrorAction SilentlyContinue
        # Verificar que el cambio tomó efecto
        $verify = Get-ItemProperty -Path $matchedKey.PSPath -Name $prop -ErrorAction SilentlyContinue
        if ($null -ne $verify -and $verify.$prop -eq '0') {
            $changesMade++
        }
    }
}
$optimized.Add([PSCustomObject]@{ Name = $adapter.Name; ChangesMade = $changesMade })
```

**Cambiar el tipo de `$optimized`** de `List[string]` a `List[PSCustomObject]`:
```powershell
[System.Collections.Generic.List[PSCustomObject]] $optimized = [System.Collections.Generic.List[PSCustomObject]]::new()
```

**Resultado final** — mantener la misma clave `AdaptersOptimized` pero ahora contiene objetos (retrocompatible con main.ps1 si se actualiza en Task 2):
```powershell
return [PSCustomObject]@{
    AdaptersOptimized = [PSCustomObject[]] $optimized.ToArray()
    Success           = [bool] $overallSuccess
}
```

**Caso sin matchedKey** (adaptador sin clave de registro): agregar al optimized con ChangesMade=0:
```powershell
} else {
    $optimized.Add([PSCustomObject]@{ Name = $adapter.Name; ChangesMade = 0 })
}
```
  </action>
  <verify>
    `powershell -NoProfile -Command "& { . .\modules\Network.ps1; 'OK' }"` sin errores.
    `Select-String -Path modules\Network.ps1 -Pattern 'ChangesMade'` — debe aparecer al menos 3 veces.
  </verify>
  <done>Optimize-Network retorna AdaptersOptimized como PSCustomObject[] con Name y ChangesMade. Sin errores de StrictMode.</done>
</task>

<task type="auto">
  <name>Task 2: UI actualizada en main.ps1 para mostrar ChangesMade</name>
  <files>main.ps1</files>
  <action>
En el bloque de resultado de red (después de `Wait-ToolkitJobs`), reemplazar el foreach que imprime adaptadores:

**Actual:**
```powershell
foreach ($ad in $result.AdaptersOptimized) {
    Write-Host ("  [OK] Adaptador : {0}" -f $ad) -ForegroundColor Green
}
Write-Host '  [OK] TCP Global Settings + DNS Flush' -ForegroundColor Green
```

**Nuevo:**
```powershell
foreach ($ad in $result.AdaptersOptimized) {
    if ($ad.ChangesMade -gt 0) {
        Write-Host ("  [OK] {0,-28} {1} propiedades aplicadas" -f $ad.Name, $ad.ChangesMade) -ForegroundColor Green
    } else {
        Write-Host ("  [-] {0,-28} sin cambios (propiedades no encontradas en driver)" -f $ad.Name) -ForegroundColor DarkYellow
    }
}
Write-Host '  [OK] TCP Global Settings + DNS Flush' -ForegroundColor Green
```
  </action>
  <verify>
    `Select-String -Path main.ps1 -Pattern 'ChangesMade'` — debe aparecer al menos una línea.
    `Select-String -Path main.ps1 -Pattern 'propiedades aplicadas'` — debe existir.
  </verify>
  <done>main.ps1 muestra conteo de propiedades por adaptador. Sin errores de parseo.</done>
</task>

</tasks>

<verification>
- `powershell -NoProfile -Command "& { . .\modules\Network.ps1; 'OK' }"` — sin errores
- `Select-String -Path modules\Network.ps1 -Pattern 'ChangesMade'` — 3+ matches
- `Select-String -Path main.ps1 -Pattern 'propiedades aplicadas'` — 1 match
- Objeto retornado por Optimize-Network: AdaptersOptimized[0].Name y AdaptersOptimized[0].ChangesMade existen
</verification>

<success_criteria>
- Optimize-Network retorna PSCustomObject[] con Name + ChangesMade por adaptador
- Verificación post-apply via read-back del registro
- La UI distingue entre adaptadores con cambios reales vs sin propiedades en el driver
- Sin regresiones en StrictMode
</success_criteria>

<output>
Después de completar, crear `.gsd/phases/06-network-module-review/06-02-SUMMARY.md`
</output>
